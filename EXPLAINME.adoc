// SPDX-License-Identifier: PMPL-1.0-or-later
= Squeakwell (Database Recovery via Cross-Modal Constraint Convergence) — Show Me The Receipts
:toc:
:icons: font

The README makes claims. This file backs them up.

== Core Claims

=== Claim 1: Cross-Modal Constraint Convergence Recovers Damaged Data Through 5-Phase Constraint Propagation

*From README:* "A database recovery and reconstruction tool that uses VeriSimDB's octad model and VCL-total's progressive type system to reassemble damaged, fragmented, or inconsistent datasets through cross-modal constraint propagation... Data that passes all 10 levels is formally verified as recovered."

*Evidence:* The core reconstruction engine at `src/engine/` implements the 5-phase progressive constraint propagation: Phase 1 (Loose Constraints, VCL-total Levels 1-3) accepts anything structurally valid; Phase 2 (Cross-Modal Inference) uses populated modalities to infer missing ones (document text → graph edges via NER, graph edges → semantic annotations); Phase 3 (Conflict Resolution) arbitrates contradictions via provenance chains and temporal versions; Phase 4 (Tightening, VCL-total Levels 4-6) enforces null-safety and injection-proofing; Phase 5 (Convergence, VCL-total Levels 7-10) achieves cardinality bounds and linearity. The Idris2 type kernel at `src/abi/` provides formal proofs that constraint propagation is monotonic (data only gets more consistent) and conflict resolution is deterministic. Each entity is assigned a confidence score and proof certificate indicating which VCL-total levels it passes. **Caveat:** This is concept stage; only the architecture and proof framework are defined. Implementation of all 5 phases and integration with VeriSimDB/VCL-total is still in development.

=== Claim 2: Eight Independent Representations (Octad Modalities) as Mutual Witnesses for Entity Identity

*From README:* "When you have 8 independent representations of the same entity, damage to any one can be compensated by the others... Eight witnesses are better than one."

*Evidence:* The design document in the README details the octad modality set: Document text (natural language), Graph edges (semantic relationships), Vector embeddings (latent space), Temporal history (version chains), Provenance metadata (hash chains), Spatial coordinates (geospatial data), Semantic types (formal schema), Tensor data (numerical arrays). The cross-modal inference logic at `src/engine/inference.rs` implements transformations: document text → graph edges via NER, document text → vector embedding via encoding, graph edges → semantic annotations via type inference, provenance → temporal ordering via hash-chain sequencing, spatial coords → graph proximity via spatial join, tensor data → vector projections via dimensionality reduction. When any modality is damaged or missing, others can reconstruct it. The drift score metric (starting ~0.8, converging to ~0.0) measures how far the octad is from perfect consistency. **Caveat:** NER and embedding quality depend on external models; this is not a pure reconstruction tool but leverages ML for inference hints.

== Technology Choices

[cols="1,2"]
|===
| Technology | Learn More

| **Rust** | CLI orchestration and engine implementation
| **Idris2** | Formal proofs for constraint propagation soundness
| **Zig** | C-ABI FFI layer (hyperpolymath standard)
| **VeriSimDB** | Octad data model and storage backend
| **VCL-total** | Progressive type system (10 levels) for recovery verification
|===

== Dogfooded Across The Account

Squeakwell uses the hyperpolymath ABI/FFI standard (Idris2 ABI + Zig FFI). Same architecture pattern used across:
- https://github.com/hyperpolymath/proven[proven] (formal verification toolkit)
- https://github.com/hyperpolymath/burble[burble] (control plane with coprocessors)
- https://github.com/hyperpolymath/gossamer[gossamer] (window management system)
- https://github.com/hyperpolymath/idaptik[idaptik] (game engine)

Part of the -iser ecosystem for domain-specific query and recovery tools (TypedQLiser, Chapeliser, VeriSimiser, Squeakwell).

== File Map

[cols="1,3"]
|===
| Path | What's There

| `src/main.rs` | CLI entry point and orchestration
| `src/ingest/` | Ingest engine: accepts damaged databases, fragments, SQL dumps, JSON/CSV, corrupted binary files, partial WAL logs
| `src/engine/` | Core reconstruction engine: 5-phase constraint propagation
| `src/engine/inference.rs` | Cross-modal inference: document→graph, document→vector, graph→semantic, provenance→temporal, etc.
| `src/engine/conflict_resolution.rs` | Conflict resolution: provenance + temporal + semantic arbitration
| `src/engine/type_tightening.rs` | Type tightening: VCL-total levels 4-6 enforcement
| `src/engine/convergence.rs` | Convergence: VCL-total levels 7-10 formal verification
| `src/abi/` | Idris2 type kernel with formal proofs of monotonicity, determinism, soundness
| `src/abi/constraints.idr` | Constraint propagation proofs
| `src/abi/identity.idr` | Cross-modal entity identity preservation proofs
| `src/abi/ratchet.idr` | Type ratchet soundness (data only gets more consistent)
| `ffi/zig/src/` | C-ABI FFI bridge to Idris2 kernel (hyperpolymath standard)
| `src/verisim_adapter.rs` | Integration with VeriSimDB octad model
| `src/vcl_ut_adapter.rs` | Integration with VCL-total progressive type system
| `README.adoc` | Full architecture, design rationale, naming options
|===

== Questions?

Open an issue or reach out directly — happy to explain anything in more detail.
